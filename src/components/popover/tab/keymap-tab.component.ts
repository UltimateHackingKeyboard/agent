import { Component, OnInit } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'keymap-tab',
    template:
    `
        Keymap
    `
})
export class KeymapTabComponent implements OnInit {
    constructor() { }

    ngOnInit() { }

}
